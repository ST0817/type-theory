[package]
name = "repl"
version = "0.1.0"
edition = "2024"

[dependencies]
ariadne.workspace = true
parsers = { version = "0.1.0", path = "../../crates/parsers" }
rustyline = "18.0.0"
simply_typed_lambda_calculus = { path = "../../crates/simply_typed_lambda_calculus" }
